# Example secrets file - copy to secrets.yml and encrypt with ansible-vault
# Command: ansible-vault encrypt deploy/ansible/vars/secrets.yml

# Database password
postgres_password: "your-secure-password-here"

# OpenAI API key (for pgai vectorizer)
openai_api_key: "sk-your-openai-key-here"

# Let's Encrypt email for SSL certificate notifications
letsencrypt_email: "mohamedalichelbi123@gmail.com"
