# Ansible inventory - defines your servers
all:
  hosts:
    database:
      ansible_host: "{{ server_ip }}"  # Will be set when running playbook
      ansible_user: root
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
